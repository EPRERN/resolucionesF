/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { ResolucionesyearService } from './resolucionesyear.service';

describe('Service: Resolucionesyear', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ResolucionesyearService]
    });
  });

  it('should ...', inject([ResolucionesyearService], (service: ResolucionesyearService) => {
    expect(service).toBeTruthy();
  }));
});
